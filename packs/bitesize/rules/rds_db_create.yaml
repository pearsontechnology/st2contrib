---
name: "bitesize_rds_create"
pack: "bitesize"
description: "Check for rds events to manage from kubernetes thirdpartyresource sensor"
enabled: true

trigger:
  type: "kubernetes.thirdpartyobject"
  parameters: {}

criteria:
  trigger.resource:
    type: "contains"
    pattern: "ADDED"
  trigger.object_kind:
    type: "equals"
    pattern: "Mysql"

action:
  ref: "bitesize.db_create_chain"
  parameters:
    payload: "{{trigger}}"
