---
name: "cassandra_delete"
pack: "bitesize"
description: "Check for Cassandra cloudformation stacks to delete using kubernetes thirdpartyresource sensor"
enabled: true

trigger:
  type: "kubernetes.thirdpartyobject"
  parameters: {}

criteria:
  trigger.resource:
    type: "contains"
    pattern: "DELETED"
  trigger.labels.type:
    type: "equals"
    pattern: "cassandra.prsn.io"

action:
  ref: "bitesize.cassandra_delete_chain"
  parameters:
    payload: "{{trigger}}"
