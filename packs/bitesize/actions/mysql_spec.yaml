---
  name: "mysql_spec"
  entry_point: "mysql_spec.py"
  pack: "bitesize"
  description: "Munge Kubernetes data so we can create a database."
  runner_type: run-python
  parameters:
    payload:
      type: object
    config:
      type: object
