---
  name: "setup_ns"
  runner_type: "action-chain"
  description: "Setup kubernetes namespace once approved"
  enabled: true
  entry_point: "workflows/setup_ns.yaml"
  pack: "bitesize"
  parameters:
    payload:
      type: "object"
      description: "payload object from kubernetes sensor"
