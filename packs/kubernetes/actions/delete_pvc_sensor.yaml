---
  name: "delete_pvc_sensor"
  runner_type: "run-python"
  description: "Delete PVC sensor"
  enabled: true
  entry_point: "delete_pvc_sensor.py"
  pack: "kubernetes"
  parameters:
    payload:
      type: "object"
      description: "payload object from kubernetes sensor"
